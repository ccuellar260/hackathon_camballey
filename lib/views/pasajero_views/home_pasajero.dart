import 'package:flutter/material.dart';

class HomePasajero extends StatelessWidget {
  const HomePasajero({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("vista Home Pasajero");
  }
}